# Additional tools installed on top of kali-linux-default.
# One apt package name per line. Blank lines and comments are ignored.
# Edit freely — re-run with: ./pt-ai provision

burpsuite
crackmapexec
evil-winrm
ffuf
gobuster
impacket-scripts
ligolo-ng
netexec
responder
seclists
sqlmap

# AD / web / mobile / wireless (apt; present in Kali repos — verified 2026-06-14)
apktool
jadx
bettercap
hostapd
bloodhound
certipy-ad
subfinder
enum4linux-ng

# Cloud / Kubernetes (apt). The cloud-audit toolset proper, and the CLIs with no
# apt package (aws, prowler, scoutsuite, trufflehog, gcloud, gitleaks, kubeaudit),
# live in provision/06-cloud.sh.
pacu
kubectl
trivy
azure-cli
