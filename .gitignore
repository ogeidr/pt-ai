# macOS folder metadata
.DS_Store
# Windows folder metadata
Thumbs.db
# Vim swap files
*.swp
*.swo
*~
# API keys and secrets
.env
.env.local
# Node dependencies
node_modules/
# JetBrains IDE settings
.idea/
# VS Code settings
.vscode/
# Runtime logs
*.log
# Claude Flow state
.claude-flow/
# Local Claude config and agents
.claude/
# Engagement evidence (sensitive)
engagements/
# Plugin test-suite runtime artifacts (logs/summary — not source)
test/results/
# Security review outputs (sensitive)
reviews/
# Features (sensitive)
features/
vagrant/config/engagement.env
# Local-model (LM Studio/Ollama) endpoint — may carry an internal IP; .example stays tracked
vagrant/config/opencode/local-model.json
